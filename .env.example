# 🔧 Svelte + Go Starter Kit Environment Configuration
# =====================================================
# 
# Copy this file to .env and customize as needed
# cp .env.example .env

# 🌐 Server Configuration
# ----------------------
PORT=8080                    # Main server port (default: 8080)
HOST=0.0.0.0                # Server host (default: 0.0.0.0 for all interfaces)

# 🗄️ Database Configuration  
# -------------------------
DB_PATH=./data/badger        # BadgerDB storage path (default: ./data/badger)
DB_LOG_LEVEL=ERROR           # Database log level: DEBUG, INFO, WARN, ERROR (default: ERROR)

# 📡 NATS Configuration
# --------------------
NATS_PORT=4223              # NATS server port (default: 4223)
NATS_HOST=127.0.0.1         # NATS server host (default: 127.0.0.1)
NATS_STORE_DIR=./data/jetstream  # JetStream storage path (default: ./data/jetstream)

# 🎯 Development Configuration
# ----------------------------
ENV=production              # Environment: development, production (default: production)
LOG_LEVEL=INFO              # Application log level: DEBUG, INFO, WARN, ERROR (default: INFO)

# 🔒 Security Configuration (Optional)
# -----------------------------------
# JWT_SECRET=your-secret-key-here     # JWT signing secret (generate a secure random string)
# CORS_ORIGINS=http://localhost:3000  # Allowed CORS origins (comma-separated)
# RATE_LIMIT=100                      # Requests per minute per IP (default: unlimited)

# 📊 Monitoring Configuration (Optional)
# -------------------------------------
# METRICS_ENABLED=true               # Enable metrics collection (default: false)
# HEALTH_CHECK_INTERVAL=30s          # Health check interval (default: 30s)

# 🚀 Production Configuration (Optional)
# -------------------------------------
# GIN_MODE=release                   # Gin framework mode: debug, release (default: release in production)
# GRACEFUL_SHUTDOWN_TIMEOUT=30s      # Graceful shutdown timeout (default: 30s)

# 📧 Email Configuration (Example - if you add email features)
# ----------------------------------------------------------
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password

# ☁️ Cloud Storage Configuration (Example - if you add file uploads)
# ----------------------------------------------------------------
# AWS_REGION=us-east-1
# AWS_ACCESS_KEY_ID=your-access-key
# AWS_SECRET_ACCESS_KEY=your-secret-key
# S3_BUCKET=your-bucket-name

# 📝 Notes:
# --------
# • Environment variables override default values
# • Bun automatically loads .env files (no dotenv package needed)
# • In production, set these as system environment variables for security
# • The application works perfectly with defaults - this file is optional
# • BadgerDB and NATS will create directories automatically if they don't exist